#include <iostream>
using namespace std;
int main()
{
	float a,b;
	cout<<"nhap 2 so a va b ";
	cin>>a>>b;

	if (a>b) {cout<<"so lon hon la: a = "<<a;}
	else if (a==b) {cout<<"a = b";}
	else {cout<<"so lon hon la: b = "<<b;}
} 
